target/

# generated proof objects (re-emitted by the binaries; large)
*.out.mm
data/birkhoff.out.mm
data/grounded.out.mm
# Obligation-O measurement corpus: a 132 MB re-emission of the canonical
# grounded corpus (prefix-renamed); regenerated by `cargo run --bin
# sdgobligbuild`, MEASURED by `sdgobligfloor`. Same discipline as
# data/grounded.out.mm — a build artifact, never tracked. The MEASURED
# bound (16,476) is recorded in COST_STRUCTURE.md with the repro recipe.
data/sdg.oblig.mm

# vendored third-party corpus, fetched on demand (public domain, ~tens of MB)
data/set.mm

# but DO ship the gzipped genuine no-cheating DB for the in-browser
# re-verify (810 KB; the shared-DAG proof, not the cut-free expansion)
!docs/data/grounded.out.mm.gz

# verified factored corpus (regenerated by metasearch s2 / cse)
data/grounded.factored.mm
