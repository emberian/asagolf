# Stage 2 — native RCF-in-ZFC: scope (the big lever, now bounded)

## Goal

Collapse the **transport bridge** (~10^46, EXTRACTED) — the only large
number left — by formalizing a model of F1 *natively in ZFC* and binding
F1-satisfaction to **set.mm's genuine ZF base** (the shared 13-axiom
FOL+ZF core the #9/substrate analysis isolated), **not** to `CCfld` /
analytic ℝ. set.mm's ~10^46 is an artifact of *its* construction
choices; a native ZF model pays only the ZF-set construction, not the
analytic-ℝ tower.

## The scope collapse (why this is now bounded, not open-ended)

Stage 1 MEASURED that the closed ASA′ proof forces exactly **one
distinct, un-nested radical**: `( sqrt ( u * ( inv ( sqd a c ) ) ) )`,
K = 1, nesting depth 1. Consequence: the model the proof needs is **not**
the full Euclidean closure of ℚ and **not** real-closed-field theory.
It is:

    ℚ_geo  ⊕  exactly ONE quadratic extension  ℚ_geo(√r)

where `r` is that single measured radicand. This removes the open-ended
"formalize all of RCF" risk. We formalize a *finite, specific* tower of
height 1 over a directly-built ℚ.

## Milestones (each measured-or-projected, labelled; iron rule applies)

- **A — ℚ from ZF, natively.** ℕ→ℤ→ℚ as ZF sets (ω, sign/pair classes),
  ℚ an ordered field, *without* `CCfld`. This is the #9 ℤ→ℚ
  pair-plumbing, built directly and minimally. The dominant term;
  measure its cut-free cost in OUR kernel. (Real work — honest about
  that.)
- **B — the one quadratic extension.** ℚ_geo(√r) = ℚ_geo[x]/(x²−r) as a
  ZF set + ordered field with √r present. Reuse the *already MEASURED*
  generic unit step (10^2.149), instantiated at the Stage-1 radicand.
- **C — transport binding.** Kernel/extractor-checked proof that this ZF
  object satisfies F1, bound to set.mm's **genuine ZF base** (ax-1..12,
  ax-mp, ax-gen, ax-ext — the 13 shared by every twin), NOT `CCfld`.
  Measure/extract its cost; this is the number that replaces ~10^46.

## Honest discipline

- Every digit kernel-MEASURED or extractor-EXACT; anything else
  `PROJECTION:` with derivation, never merged into a measured line.
- Three quantities still strictly separate (geometry · grounding ·
  satisfiability). Stage 2 only moves the *satisfiability/transport*
  number; it does not touch the 10^5.58 geometry or the grounding line.
- Transport-anchored rule ([[transport-anchored-floor]]): the number is
  only claimable if the binding to set.mm's ZF base is machine-checked,
  not assumed. If a native ℚ-from-ZF turns out to itself be expensive in
  set.mm terms, report that honestly — it would still sharpen the
  thesis, not fake a win.
- This is a *project*, staged. "Begin" = Milestone A underway with an
  honest measured/projected checkpoint; completion is not promised in
  one pass.

## Status

- Stage 1: DONE — K=1 MEASURED, construction projection removed
  (commit aed0213).
- Stage 2 Milestone A: DONE — native ℚ-from-ZF (ω→ℤ→ℚ, no CCfld /
  Dedekind / analytic ℝ). `verified all 11 $p in data/qzf.mm ✔`;
  ordered-field consequences F1 needs MEASURED at **10^2.408**.
  Honest hedge (same discipline as euclid.mm): the abstract
  semiring/ring/ordered-field *signature* (`ax-N*/ax-Z*/ax-Q*`) is
  `$a`-asserted; fully discharging it to bare ZF (quotient
  well-definedness) is a **labelled PROJECTION ≤10^4**, never merged
  into the measured number. Even that pessimistic bound is ~40 orders
  below set.mm's ℚ-through-`CCfld` (10^46.10 EXTRACTED). Confirms the
  thesis: the irreducible ~10^46 is set.mm's analytic-ℝ routing
  choice, not a property of ℚ or F1.
- Stage 2 Milestone B: DONE — ℚ_geo(√r) at the Stage-1 radicand (K=1),
  `verified all 3 $p in data/qzfext.mm ✔`, MEASURED **10^2.149** (reuses
  the euclid unit; B makes √-satisfaction a *kernel-proved theorem* over
  the native carrier — NOT bound to set.mm `resqrtth`).
- Stage 2 Milestone C: DONE — transport binding of the native model
  (qzf ⊕ qzfext) to set.mm's **genuine 13-axiom ZF base**, full
  proof-DAG audit: every ZF primitive (`0ex/sucexg/omex/opex/zfpair2/
  uniex/axextg`) reaches the bare ZF base with **ZERO analytic-ℝ/CCfld
  in the DAG**. Transport-anchored native-model floor = max(A 10^2.408,
  B 10^2.149, ZF-bind 10^17.484) = **10^17.484 [EXTRACTED-dominated;
  A/B MEASURED]**. The ≤10^4 asserted-signature projection sits *below*
  the floor → non-binding, still labelled/separate.
- **VERDICT: the ~10^46 COLLAPSES to 10^17.484** (machine-checked,
  ~10^28.6 orders). The irreducible residual is **not analytic ℝ** — it
  is `omex` (the Axiom of Infinity: ω is a set), the heaviest bare-ZF
  primitive the ω→ℕ→ℤ→ℚ tower needs. The ~10^46 was set.mm's analytic-ℝ
  *routing choice for √*; a native ZF model with kernel-proved
  √-satisfaction reduces the substrate to the genuine cost of "ℕ
  exists." Stage 2 COMPLETE.
